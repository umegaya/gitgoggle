source 'https://rubygems.org'

group :commit_hook do
  # json
  gem 'json', group: :commit_hook
  # thin
  gem 'thin', group: :commit_hook
  # rack
  gem 'rack', group: :commit_hook
  # sinatra
  gem 'sinatra', group: :commit_hook
end

